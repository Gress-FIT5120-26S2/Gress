# This file contains no secret. Copy it to the untracked .env.production before a production build,
# or set the same public value in the EAS production environment.
EXPO_PUBLIC_API_URL=https://your-production-api.example.com
